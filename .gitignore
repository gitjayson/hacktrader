# --- Local helper scripts (mac-specific, not source) ---
sync_hacktrader.sh
deploy.sh
setup_git.sh
.pengo_tmp

# --- composer dev dependencies (installed locally, not deployed) ---
vendor/

# --- dev binaries downloaded by `make install` ---
bin/phpstan.phar
bin/phpunit.phar
bin/

# --- pytest venv ---
.venv/

# --- phpunit cache ---
.phpunit.cache/

# --- Secrets / credentials ---
api-accounts.json
api-accounts.sqlite
users.sqlite
users.sqlite-shm
users.sqlite-wal
.env
.env.*

# --- Live runtime state (changes constantly, not source) ---
api-usage.json
api_usage_tracker.json
correlations.json
correlation-status.json
correlation-locks/
focus-universe.json
market-watchlist.json

# --- Backups ---
*.bak
*.bak-*
backups/

# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
.ruff_cache/
.mypy_cache/
.pytest_cache/

# --- Logs / cache / temp ---
*.log
*.log.*
logs/
log/
cache/
caches/
.cache/
tmp/
temp/

# --- macOS / editors ---
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# --- Pipelines: per-ticker analyzer output, regenerated on every run ---
pipelines/

# --- Server-side health probe state ---
state/health-status.json

# --- Nginx default landing page on Ubuntu, irrelevant to the repo ---
index.nginx-debian.html

# --- Defensive: a nested clone of this repo (e.g. accidental
#     `git clone https://github.com/...hacktrader.git` from inside the
#     working tree) showed up in a code review as ~6.9MB of untracked
#     phantom content. Block it from ever being staged. Remove the
#     directory manually if it exists. ---
/hacktrader/

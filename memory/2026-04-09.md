# 2026-04-09
- Built HackTrader v0.7.2.5 in the workspace.
- Added a top-right attributed API usage card to `dashboard.php` showing per-user request count, success rate, error count, and last counted scan.
- Updated `api.php` so market-data responses include a per-session usage summary for the dashboard UI.
- Bumped visible version markers to v0.7.2.5 in `dashboard.php`, `index.php`, `README.md`, `VERSION.md`, and usage-tracker metadata emission in `run-brk.py`.
- Deployed updated files (`dashboard.php`, `api.php`, `index.php`, `run-brk.py`, `README.md`, `VERSION.md`) to `dev.hacktrader.com:/var/www/html/` after backing up the previous server files to `/var/www/html/backups/20260409-124450`.
- Refined the UI into HackTrader v0.7.2.6 by moving the API usage widget out of the header and into the dashboard's actual top-right side panel for a cleaner cockpit layout.
- Deployed the v0.7.2.6 UI fix to `dev.hacktrader.com:/var/www/html/` after backing up the prior files to `/var/www/html/backups/20260409-140901`.
- After reviewing a dashboard screenshot in the workspace, further refined the layout into HackTrader v0.7.2.7 by integrating the API usage widget into the top-right context card, reducing floating empty space, tightening control-bar spacing, and clarifying zero-usage empty states.
- Deployed the v0.7.2.7 UI fix to `dev.hacktrader.com:/var/www/html/` after backing up the prior files to `/var/www/html/backups/20260409-141134`.
- Added HackTrader v0.7.2.8 with a mirrored probe-pressure graph in Attempt monitor so failed upside and downside probes are visible at a glance.
- Deployed the v0.7.2.8 update to `dev.hacktrader.com:/var/www/html/` after backing up the prior files to `/var/www/html/backups/20260409-203156`.
- After reviewing another dashboard screenshot, adjusted the layout again in HackTrader v0.7.2.9 so the probe-pressure graph renders higher in the top-right context card instead of hiding near the bottom of the right column.
- Deployed the v0.7.2.9 update to `dev.hacktrader.com:/var/www/html/` after backing up the prior files to `/var/www/html/backups/20260409-204051`.
- Remote PHP lint passed for `dashboard.php`, `api.php`, and `index.php`, and `python3 -m py_compile` passed for `run-brk.py` on the dev server.

# 2026-04-08
- Completed HackTrader v0.7.2.4 in the workspace.
- Added persistent `session:user_name` identity mapping tied to Google-authenticated email in `callback.php` using `state/user-identities.json`.
- Added per-session Twelve Data accounting in `api_usage_tracker.json` via `run-brk.py` and requester identity propagation from `api.php`.
- Preserved the newer dev-server auth flow by merging v0.7.2.4 changes into the live-style `api.php` and adding `api_auth.php` to the repo.
- Updated visible version markers in `dashboard.php`, `index.php`, `README.md`, and `VERSION.md` to v0.7.2.4.
- Created and refreshed a `hacktrader-v0.7.2.4/` snapshot folder for the release.
- Deployed the merged v0.7.2.4 files to `dev.hacktrader.com` after backing up the prior server files to `/var/www/html/backups/v0.7.2.4-20260408-183029`.
- Remote PHP lint passed for `api.php`, `api_auth.php`, `callback.php`, `dashboard.php`, and `index.php`, and `python3 -m py_compile` passed for `run-brk.py`.
- Earlier local commit `50cd2ab` was superseded by later deploy-ready fixes.

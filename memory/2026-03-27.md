# 2026-03-27

- Jay provided new HackTrader dev server details for deploying v0.7.0.
- **Host:** `dev.hacktrader.com`
- **SSH User:** `agent`
- **SSH Key:** `~/.ssh/id_ed25519_hacktrader`
- **SSH Command:** `ssh -i ~/.ssh/id_ed25519_hacktrader agent@dev.hacktrader.com`
- **Web Root:** `/var/www/html`
- **Server State:** Fresh install; nothing needs to be preserved.
- Deployed `hacktrader-v0.7.0` to `dev.hacktrader.com` at `/var/www/html`.
- Installed PHP 8.3 + PHP-FPM on the Ubuntu 24.04 dev server and configured nginx to serve `index.php` with FastCGI.
- Verified `http://dev.hacktrader.com/` returns the HackTrader sign-in page and `dashboard.php` redirects to `index.php` when unauthenticated.
- Set web root ownership to `www-data:www-data` with directories `755`, standard files `644`, and executable `.py`/`.sh` scripts `755`.
- Updated HackTrader dev auth/layout to use `/var/www/secrets.json` and `/var/www/vendor/`, which are referenced from PHP as `../secrets.json` and `../vendor/` relative to `/var/www/html`.
- Synced local `secrets.json` to `/var/www/secrets.json` (mode `640`) and local `vendor/` to `/var/www/vendor/`, then updated `callback.php` accordingly.
- Verified `http://dev.hacktrader.com/callback.php` redirects to Google OAuth and the callback PHP can load both autoload/vendor and secrets successfully.
